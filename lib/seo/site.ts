// The domain itself is infrastructure, not content — it stays a constant here
// rather than moving into SiteSettings (lib/settings/repository.ts), which
// owns the studio's actual editable info (contact, social links, SEO
// defaults).
export const SITE_URL = "https://diamantes3designs.com";
